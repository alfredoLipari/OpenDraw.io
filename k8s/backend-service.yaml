---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: flask-app-service
  name: flask-app-service
spec:
  selector:
    tier: backend
    track: stable
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
...